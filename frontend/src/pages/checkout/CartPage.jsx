function CartPage() {
  return (
    <main>
      <h1>Carrito</h1>
      <section aria-labelledby="carrito-productos-agregados">
        <h2 id="carrito-productos-agregados">Productos agregados</h2>
      </section>
      <section aria-labelledby="carrito-cantidades">
        <h2 id="carrito-cantidades">Cantidades</h2>
      </section>
      <section aria-labelledby="carrito-subtotal">
        <h2 id="carrito-subtotal">Subtotal</h2>
      </section>
      <section aria-labelledby="carrito-boton-para-continuar-al-checkout">
        <h2 id="carrito-boton-para-continuar-al-checkout">Botón para continuar al checkout</h2>
      </section>
    </main>
  )
}

export default CartPage
